# Data

The raw data for the stimulus rating study are stored in the project's central data directory:

```
data/raw/PageTimes-2026-04-22.csv
data/raw/ratings_2026-04-22.csv
```

The macOS alias files in this directory point to those canonical copies and exist only for local convenience. Any analysis should read from `data/raw/` directly.